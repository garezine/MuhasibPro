<Project>
  <PropertyGroup>
    <!-- Platform ve mimari ayarları -->
    <Platform>x64</Platform>
    <PlatformTarget>x64</PlatformTarget>
    <PreferredToolArchitecture>x64</PreferredToolArchitecture>

    <AppxAutoInjectionPackageExtensions>false</AppxAutoInjectionPackageExtensions>
    <AppxPackage>false</AppxPackage>
    <WindowsPackageType>None</WindowsPackageType>

    <!-- Çıkış dizinlerini platforma göre ayırma -->
    <OutputPath>bin\$(Platform)\$(Configuration)\</OutputPath>
    <IntermediateOutputPath>obj\$(Platform)\$(Configuration)\</IntermediateOutputPath>

    <WindowsAppSDKGeneratePackage>false</WindowsAppSDKGeneratePackage>
    <EnableMsixTooling>false</EnableMsixTooling>
  </PropertyGroup>

  <!-- ✅ CI/Publish durumunda self-contained, aksi halde framework-dependent -->
  <PropertyGroup Condition="'$(CI)' == 'true' OR '$(PublishDir)' != ''">
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <SelfContained>true</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>

  <PropertyGroup Condition="'$(CI)' != 'true' AND '$(PublishDir)' == ''">
    <WindowsAppSDKSelfContained>false</WindowsAppSDKSelfContained>
    <SelfContained>false</SelfContained>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <DefineDebug>true</DefineDebug>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <DefineDebug>false</DefineDebug>
  </PropertyGroup>
</Project>
