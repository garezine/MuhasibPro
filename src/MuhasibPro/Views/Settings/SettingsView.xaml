<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MuhasibPro.Views.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MuhasibPro.Views.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded">
    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
        <Style
            x:Key="SettingsSectionHeaderTextBlockStyle"
            BasedOn="{StaticResource BodyStrongTextBlockStyle}"
            TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="175" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="PageHeader"
            MaxWidth="1064"
            Margin="36,24,36,0"
            AutomationProperties.HeadingLevel="Level1"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Ayarlar" />
        <Grid Grid.Row="1" HorizontalAlignment="Left" Padding="36 20 36 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />                
            </Grid.ColumnDefinitions>
            <Border Margin="36 24 36 0" Style="{ThemeResource GlassPanel}" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image
                        Width="75"
                        Height="75"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Source="/Assets/Square150x150Logo.png"
                        Stretch="Fill" 
                        />
                    <StackPanel Grid.Column="1" Margin="20 0 20 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="18"
                            Style="{ThemeResource CardSuperHeader}"
                            Text="Firma :" />
                        <TextBlock
                            FontSize="16"
                            Style="{ThemeResource CompactSectionHeader}"
                            Text="Korkut Mermer Sanayi"
                            FontWeight="Thin"/>
                        <TextBlock                            
                            FontSize="18"
                            Style="{ThemeResource CardSuperHeader}"
                            Text="Çalışma Dönemi :" />
                        <TextBlock
                            FontSize="16"
                            Style="{ThemeResource CompactSectionHeader}"
                            Text="2025"
                            FontWeight="Thin"/>
                    </StackPanel>
                </Grid>

            </Border>
            <Border Grid.Column="1" Margin="36 24 36 0" Style="{ThemeResource GlassPanel}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                </Grid>
            </Border>
        </Grid>
        <ScrollView
            Grid.Row="2"
            Padding="36,0,36,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Border>
                <StackPanel MaxWidth="1064" Spacing="{StaticResource SettingsCardSpacing}">
                    <!--<StackPanel.ChildrenTransitions>
                        <RepositionThemeTransition />
                    </StackPanel.ChildrenTransitions>-->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Görünüm &amp; Davranış" />
                    <toolkit:SettingsCard Description="Uygulanacak temayı seçin" Header="Uygulama Teması">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE790;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox
                            x:Name="themeMode"
                            AutomationProperties.AutomationId="themeModeComboBox"
                            SelectionChanged="themeMode_SelectionChanged">
                            <ComboBoxItem Content="Açık" Tag="Light" />
                            <ComboBoxItem Content="Koyu" Tag="Dark" />
                            <ComboBoxItem Content="Windows Teması" Tag="Default" />

                        </ComboBox>
                    </toolkit:SettingsCard>
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Hakkında" />
                    <toolkit:SettingsExpander
                        Margin="0, 0, 0,24"
                        Description="© 2025 MuhasibPro. Tüm hakları saklıdır."
                        Header="MuhasibPro, Profesyonel Muhasebe">
                        <toolkit:SettingsExpander.HeaderIcon>
                            <BitmapIcon ShowAsMonochrome="False" UriSource="/Assets/Images/AppLogo.png" />
                        </toolkit:SettingsExpander.HeaderIcon>
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind Version}" />
                    </toolkit:SettingsExpander>
                </StackPanel>
            </Border>

        </ScrollView>
    </Grid>
</Page>